# 새로운 type을 정의해줄 수 있음
type DevRapers{
    name : String!
    age : Int!
    gender : String!
};

type Query{
    # !는 필수값이라는 것을 뜻함
    # 사용자가 name을 요청하면 사용자에게 String을 보낼 것이라는 뜻임
    person: DevRapers! 
};